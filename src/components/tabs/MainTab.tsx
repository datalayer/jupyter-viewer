import { JupyterFrontEndProps } from '../Landing';
import JupyterViewer from './../../JupyterViewer';

const MainTab = (props: JupyterFrontEndProps) => {
  return (
    <>
      <JupyterViewer/>
    </>
  );
}

export default MainTab;
